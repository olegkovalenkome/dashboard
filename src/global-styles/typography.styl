//Font Family
system-font-family = -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif

//Headings
heading-1(color) 
    font-size: 80px
    line-height: 86px
    font-weight: bold
    color: color

heading-2(color) 
    font-size: 60px
    line-height: 86px
    font-weight: bold
    color: color

heading-3(color) 
    font-size: 30px
    line-height: 43px
    font-weight: bold
    color: color

heading-4(color) 
    font-size: 24px
    line-height: 34px
    font-weight: bold
    color: color

// Body Text
large()
    line-height: 37px
    font-size: 24px

medium()
    line-height: 31px
    font-size: 20px

large-text(color)
    large()
    colro: color

large-text-bold(color)
    large()
    font-weight: bold
    colro: color

medium-text(color)
    medium()
    colro: color

medium-text-bold(color)
    medium()
    font-weight: bold
    colro: color
